import Button from "./Button/Button";
import Contador from "./Contador/Contador";


export {
    Button,
    Contador
}


// Cria-se um index dentro da pasta components, importando todos os components e os exportando em seguida, de forma que não fique cheio de importação (lista enorme de importações) quando for criar um component.. etc. Ver exemplo no App.js, onde Button e Contador forma importados na mesma linha.