import { createContext } from "react";

// Contexto para filmes em cartaz
export const MoviesNowContextProps = createContext([() => { }] as any)
export const MoviesNowContext = createContext('' as any)

